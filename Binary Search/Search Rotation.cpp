#include<iostream>
using namespace std;

int search_rotation()
{
    if(a[mid] == key)
        return low;
    else if(a[mid] < key)
    {
        if(a[low] < key)
        {

        }
    }
        return search_rotation(low, mid - 1, key)
    else if(a[mid] > key)
    else
}
